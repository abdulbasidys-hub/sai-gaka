// src/components/dashboard/IncomeModal.jsx
// This component is no longer used directly — income is added via AddTransactionSheet
// Keeping as a simple redirect notice; income button on dashboard now opens AddTransactionSheet
export default function IncomeModal() { return null; }
